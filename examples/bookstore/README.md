# Bookstore Example

This example is a small online bookstore project. It demonstrates realistic
read-only MySQL queries and the TypeScript builders generated by `sqlcomp`.

The generated files under `generated/` are committed expected artifacts. They are
regenerated and compared byte for byte by the examples check.
