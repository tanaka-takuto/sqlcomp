name: "[typescript] Pull Request"

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  typescript:
    name: Call generated TypeScript check
    uses: ./.github/workflows/_typescript-generated-check.yml
