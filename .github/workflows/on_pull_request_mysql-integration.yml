name: "[mysql] Pull Request"

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  mysql:
    name: Call MySQL integration
    uses: ./.github/workflows/_mysql-integration.yml
