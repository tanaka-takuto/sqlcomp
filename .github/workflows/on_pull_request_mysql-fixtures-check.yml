name: "[mysql-fixtures] Pull Request"

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  mysql-fixtures:
    name: Call MySQL fixtures check
    uses: ./.github/workflows/_mysql-fixtures-check.yml
