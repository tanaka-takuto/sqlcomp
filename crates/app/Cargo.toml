[package]
name = "sqlcomp-app"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Internal sqlcomp application use case and port crate."
repository.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
sqlcomp-core = { path = "../core" }
